// <auto-generated>
//     This code was generated by GenerateRequestCmdlets.ps1 (version 1.0.0) on 2025-10-30T09:10:29.5985683+00:00.
//     Changes to this file may cause incorrect behavior and will be lost if the code is regenerated.
// </auto-generated>

#pragma warning disable CS1591 // Missing XML comment for publicly visible type or member

using System;
using System.Text;
using System.Management.Automation;
using Microsoft.Xrm.Sdk;
using Microsoft.Xrm.Sdk.Messages;
using Microsoft.Crm.Sdk.Messages;
using Microsoft.PowerPlatform.Dataverse.Client;
using Rnwood.Dataverse.Data.PowerShell.Commands;

namespace Rnwood.Dataverse.Data.PowerShell.Commands.Sdk
{
    /// <summary>
    /// Contains the data to convert existing UTC date and time values in the database to DateOnly values.
    /// </summary>
    [System.CodeDom.Compiler.GeneratedCode("GenerateRequestCmdlets.ps1", "1.0.0")]
    [System.Diagnostics.DebuggerNonUserCode]
    [System.Diagnostics.CodeAnalysis.ExcludeFromCodeCoverage]
    [Cmdlet("Invoke", "DataverseConvertDateAndTimeBehavior", SupportsShouldProcess = true, ConfirmImpact = ConfirmImpact.Medium)]
    public class InvokeDataverseConvertDateAndTimeBehaviorCmdlet : OrganizationServiceCmdlet
    {

        

        

                /// <summary>
        /// Gets or sets a collection of entity and attributes to apply the behavior conversion on. Required
        /// </summary>

        [Parameter(HelpMessage = "Gets or sets a collection of entity and attributes to apply the behavior conversion on. Required")]
        public Microsoft.Xrm.Sdk.EntityAttributeCollection Attributes { get; set; }

        /// <summary>
        /// Gets or sets the conversion rule to apply. Required.
        /// </summary>

        [Parameter(HelpMessage = "Gets or sets the conversion rule to apply. Required.")]
        public System.String ConversionRule { get; set; }

        /// <summary>
        /// Gets the time zone code that has the requested localized name.
        /// </summary>

        [Parameter(HelpMessage = "Gets the time zone code that has the requested localized name.")]
        public System.Int32 TimeZoneCode { get; set; }

        /// <summary>
        /// Gets or sets whether to automatically convert to DateOnly value, if possible. Optional.
        /// </summary>

        [Parameter(HelpMessage = "Gets or sets whether to automatically convert to DateOnly value, if possible. Optional.")]
        public System.Boolean AutoConvert { get; set; }

                /// <summary>
        /// Processes the cmdlet request and writes the response to the pipeline.
        /// </summary>
        protected override void ProcessRecord()
        {
            base.ProcessRecord();

            var request = new Microsoft.Xrm.Sdk.Messages.ConvertDateAndTimeBehaviorRequest();
                        request.Attributes = Attributes;
            request.ConversionRule = ConversionRule;
            request.TimeZoneCode = TimeZoneCode;
            request.AutoConvert = AutoConvert;

            // Build a short serialized summary of the request for ShouldProcess output
            var requestSummary = new StringBuilder();
            try
            {
                var props = request.GetType().GetProperties(System.Reflection.BindingFlags.Public | System.Reflection.BindingFlags.Instance);
                foreach (var p in props)
                {
                    object val = null;
                    try { val = p.GetValue(request); } catch { val = null; }
                    var vstr = val == null ? "null" : val.ToString();
                    requestSummary.Append(p.Name).Append("=").Append(vstr).Append("; ");
                }
            }
            catch
            {
                // Ignore any errors while serializing the request for what-if messaging
            }

            // Emit verbose output of the serialized request when -Verbose is used
            try { WriteVerbose(requestSummary.ToString()); } catch { }

            // Support -WhatIf and -Confirm; include request summary in the action description
            if (!ShouldProcess(request.GetType().Name, requestSummary.ToString()))
            {
                return;
            }

            var response = Connection.Execute(request);
            WriteObject(response);
        }
    }
}

#pragma warning restore CS1591
