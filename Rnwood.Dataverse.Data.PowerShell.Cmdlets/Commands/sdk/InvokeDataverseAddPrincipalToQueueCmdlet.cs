// <auto-generated>
//     This code was generated by GenerateRequestCmdlets.ps1 (version 1.0.0) on 2025-10-08T21:01:48.6497453+00:00.
//     Changes to this file may cause incorrect behavior and will be lost if the code is regenerated.
// </auto-generated>

#pragma warning disable CS1591 // Missing XML comment for publicly visible type or member

using System;
using System.Text;
using System.Management.Automation;
using Microsoft.Xrm.Sdk;
using Microsoft.Xrm.Sdk.Messages;
using Microsoft.Crm.Sdk.Messages;
using Microsoft.PowerPlatform.Dataverse.Client;
using Rnwood.Dataverse.Data.PowerShell.Commands;
using Microsoft.Xrm.Sdk.Metadata;
using System.Collections;

namespace Rnwood.Dataverse.Data.PowerShell.Commands.Sdk
{
    /// <summary>
    /// Contains the data to add the specified principal to the list of queue members. If the principal is a team, add each team member to the queue.
    /// </summary>
    [System.CodeDom.Compiler.GeneratedCode("GenerateRequestCmdlets.ps1", "1.0.0")]
    [System.Diagnostics.DebuggerNonUserCode]
    [System.Diagnostics.CodeAnalysis.ExcludeFromCodeCoverage]
    [Cmdlet("Invoke", "DataverseAddPrincipalToQueue", SupportsShouldProcess = true, ConfirmImpact = ConfirmImpact.Medium)]
    public class InvokeDataverseAddPrincipalToQueueCmdlet : OrganizationServiceCmdlet
    {

                private EntityMetadataFactory entityMetadataFactory;
        private DataverseEntityConverter entityConverter;

                protected override void BeginProcessing()
        {
            base.BeginProcessing();
            entityMetadataFactory = new EntityMetadataFactory(Connection);
            entityConverter = new DataverseEntityConverter(Connection, entityMetadataFactory);
        }

                /// <summary>
        /// Gets or sets the ID of the queue. Required
        /// </summary>

        [Parameter(HelpMessage = "Gets or sets the ID of the queue. Required")]
        public System.Guid QueueId { get; set; }

        /// <summary>
        /// Gets or sets the principal to add to the queue. Required. Accepts PSObject with properties that will be converted to Entity. Use corresponding TableName parameter to specify the entity type.
        /// </summary>

        [Parameter(HelpMessage = "Gets or sets the principal to add to the queue. Required. Accepts PSObject with properties that will be converted to Entity. Use corresponding TableName parameter to specify the entity type.")]
        public PSObject Principal { get; set; }

        /// <summary>
        /// Gets or sets the principal to add to the queue. Required. The logical name of the table/entity type for the Principal parameter.
        /// </summary>

        [Parameter(HelpMessage = "Gets or sets the principal to add to the queue. Required. The logical name of the table/entity type for the Principal parameter.")]
        [ArgumentCompleter(typeof(TableNameArgumentCompleter))]
        public string PrincipalTableName { get; set; }

        /// <summary>
        /// Gets or sets the principal to add to the queue. Required. Properties to ignore when converting Principal PSObject to Entity.
        /// </summary>

        [Parameter(HelpMessage = "Gets or sets the principal to add to the queue. Required. Properties to ignore when converting Principal PSObject to Entity.")]
        [ArgumentCompleter(typeof(Rnwood.Dataverse.Data.PowerShell.Commands.PSObjectPropertyNameArgumentCompleter))]
        public string[] PrincipalIgnoreProperties { get; set; }

        /// <summary>
        /// Gets or sets the principal to add to the queue. Required. Hashtable specifying lookup columns for entity reference conversions in Principal.
        /// </summary>

        [Parameter(HelpMessage = "Gets or sets the principal to add to the queue. Required. Hashtable specifying lookup columns for entity reference conversions in Principal.")]
        public Hashtable PrincipalLookupColumns { get; set; }

                /// <summary>
        /// Processes the cmdlet request and writes the response to the pipeline.
        /// </summary>
        protected override void ProcessRecord()
        {
            base.ProcessRecord();

            var request = new Microsoft.Crm.Sdk.Messages.AddPrincipalToQueueRequest();
                        request.QueueId = QueueId;
            { var options = new ConvertToDataverseEntityOptions(); if (PrincipalIgnoreProperties != null) { foreach (string prop in PrincipalIgnoreProperties) { options.IgnoredPropertyName.Add(prop); } } if (PrincipalLookupColumns != null) { foreach (DictionaryEntry entry in PrincipalLookupColumns) { options.ColumnOptions[(string)entry.Key] = new ConvertToDataverseEntityColumnOptions() { LookupColumn = (string)entry.Value }; } } request.Principal = entityConverter.ConvertToDataverseEntity(Principal, PrincipalTableName, options); }

            // Build a short serialized summary of the request for ShouldProcess output
            var requestSummary = new StringBuilder();
            try
            {
                var props = request.GetType().GetProperties(System.Reflection.BindingFlags.Public | System.Reflection.BindingFlags.Instance);
                foreach (var p in props)
                {
                    object val = null;
                    try { val = p.GetValue(request); } catch { val = null; }
                    var vstr = val == null ? "null" : val.ToString();
                    requestSummary.Append(p.Name).Append("=").Append(vstr).Append("; ");
                }
            }
            catch
            {
                // Ignore any errors while serializing the request for what-if messaging
            }

            // Emit verbose output of the serialized request when -Verbose is used
            try { WriteVerbose(requestSummary.ToString()); } catch { }

            // Support -WhatIf and -Confirm; include request summary in the action description
            if (!ShouldProcess(request.GetType().Name, requestSummary.ToString()))
            {
                return;
            }

            var response = Connection.Execute(request);
            WriteObject(response);
        }
    }
}

#pragma warning restore CS1591
