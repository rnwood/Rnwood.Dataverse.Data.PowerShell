// <auto-generated>
//     This code was generated by GenerateRequestCmdlets.ps1 (version 1.0.0) on 2025-10-08T21:02:09.4986198+00:00.
//     Changes to this file may cause incorrect behavior and will be lost if the code is regenerated.
// </auto-generated>

#pragma warning disable CS1591 // Missing XML comment for publicly visible type or member

using System;
using System.Text;
using System.Management.Automation;
using Microsoft.Xrm.Sdk;
using Microsoft.Xrm.Sdk.Messages;
using Microsoft.Crm.Sdk.Messages;
using Microsoft.PowerPlatform.Dataverse.Client;
using Rnwood.Dataverse.Data.PowerShell.Commands;
using System.IO;

namespace Rnwood.Dataverse.Data.PowerShell.Commands.Sdk
{
    /// <summary>
    /// Contains the data needed to upload a block of data to storage.
    /// </summary>
    [System.CodeDom.Compiler.GeneratedCode("GenerateRequestCmdlets.ps1", "1.0.0")]
    [System.Diagnostics.DebuggerNonUserCode]
    [System.Diagnostics.CodeAnalysis.ExcludeFromCodeCoverage]
    [Cmdlet("Invoke", "DataverseUploadBlock", SupportsShouldProcess = true, ConfirmImpact = ConfirmImpact.Medium, DefaultParameterSetName = "Default")]
    public class InvokeDataverseUploadBlockCmdlet : OrganizationServiceCmdlet
    {

        

        

                /// <summary>
        /// A unique identifier for the block of data.
        /// </summary>

        [Parameter(HelpMessage = "A unique identifier for the block of data.")]
        public System.String BlockId { get; set; }

        /// <summary>
        /// The data block to upload.
        /// </summary>

        [Parameter(ParameterSetName = "Default", HelpMessage = "The data block to upload.")]
        public System.Byte[] BlockData { get; set; }



        /// <summary>

        /// Gets or sets the path to a file containing the data to upload.

        /// </summary>

        [Parameter(ParameterSetName = "FromFile", Mandatory = true, HelpMessage = "Gets or sets the path to a file containing the data to upload.")]

        [ValidateNotNullOrEmpty]

        public System.String InFile { get; set; }

        /// <summary>
        /// A token that uniquely identifies a sequence of related data uploads.
        /// </summary>

        [Parameter(HelpMessage = "A token that uniquely identifies a sequence of related data uploads.")]
        public System.String FileContinuationToken { get; set; }

                /// <summary>
        /// Processes the cmdlet request and writes the response to the pipeline.
        /// </summary>
        protected override void ProcessRecord()
        {
            base.ProcessRecord();

            var request = new Microsoft.Crm.Sdk.Messages.UploadBlockRequest();
                        request.BlockId = BlockId;
            // Load file if InFile parameter is specified
            byte[] blockDataData = BlockData;
            if (!string.IsNullOrEmpty(InFile))
            {
                if (!File.Exists(InFile))
                {
                    throw new FileNotFoundException($"The specified file does not exist: {InFile}");
                }
                blockDataData = File.ReadAllBytes(InFile);
            }
            request.BlockData = blockDataData;
            request.FileContinuationToken = FileContinuationToken;

            // Build a short serialized summary of the request for ShouldProcess output
            var requestSummary = new StringBuilder();
            try
            {
                var props = request.GetType().GetProperties(System.Reflection.BindingFlags.Public | System.Reflection.BindingFlags.Instance);
                foreach (var p in props)
                {
                    object val = null;
                    try { val = p.GetValue(request); } catch { val = null; }
                    var vstr = val == null ? "null" : val.ToString();
                    requestSummary.Append(p.Name).Append("=").Append(vstr).Append("; ");
                }
            }
            catch
            {
                // Ignore any errors while serializing the request for what-if messaging
            }

            // Emit verbose output of the serialized request when -Verbose is used
            try { WriteVerbose(requestSummary.ToString()); } catch { }

            // Support -WhatIf and -Confirm; include request summary in the action description
            if (!ShouldProcess(request.GetType().Name, requestSummary.ToString()))
            {
                return;
            }

            var response = Connection.Execute(request);
            WriteObject(response);
        }
    }
}

#pragma warning restore CS1591
