---
title: "Remove-DataverseEntityKeyMetadata - Preview deletion with WhatIf"
tags: ['Metadata']
source: "Remove-DataverseEntityKeyMetadata.md"
---
This command shows what would happen if the key were deleted, but doesn't actually perform the deletion.

```powershell
Remove-DataverseEntityKeyMetadata -EntityName contact -KeyName "contact_name_key" -WhatIf

```

